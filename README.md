# Fault-Tolerant-Full-Stack-Deployment-Docker-AWS-EC2
Full-stack app (React, Express, MongoDB) containerized with Docker and deployed on AWS EC2, hardened for real-world failure: DB-aware health checks, persistent storage verified against data loss, automatic container recovery after reboot, Elastic IP, and locked-down security groups.
